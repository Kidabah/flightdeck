# Amy internet access

Amy can search the live web (`web_search`) and open specific links (`fetch_url`) when workshop notes aren't enough — docs, prices, current info, etc.

She still prefers Flightdeck notes for printer/bench facts. Searches use DuckDuckGo (no extra API key).
